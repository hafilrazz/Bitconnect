name: mesh_transport
description: Transport interfaces helpers and FakeTransport for Netless.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  mesh_protocol:
    path: ../mesh_protocol
  meta: ^1.12.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
